import styles from "./styles.module.scss"

export default function Experience() {

    return <div className={`df fdircol ${styles.experience_container}`}>
        <div className={`mgr20 ${styles.experience_header}`}>Experiece</div>
        <div>
            Experience...
        </div>
    </div>
}
